version: '3'

services:
  nav-item:
    image: eooce/nav-item
    container_name: nav-item
    ports:
      - "3000:3000"
    environment:
      - PORT=3000             # 监听端口
      - ADMIN_USERNAME=yue  # 后台用户名
      - ADMIN_PASSWORD=yue6719109 # 后台密码
    volumes:
      - ./database:/app/database  # 持久化数据库文件
    restart: unless-stopped

